export enum GameNums {
  END_GAME = 0,
  STOP_COUNTDOWN = -5,
  STOP_CLICKS_COUNT = -1,
}

export enum URL {
  HOME = '/',
  GAME = '/game',
  HIGH_SCORE = '/highscore',
}

export enum Levels {
  EASY = 'easy',
  NORMAL = 'normal',
  HARD = 'hard',
}

export enum LevelValues  {
  EASY_VAL = 10,
  NORMAL_VAL = 20,
  HARD_VAL = 30,
}
